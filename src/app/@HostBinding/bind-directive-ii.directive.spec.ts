import { BindDirectiveIiDirective } from './bind-directive-ii.directive';

describe('BindDirectiveIiDirective', () => {
  it('should create an instance', () => {
    const directive = new BindDirectiveIiDirective();
    expect(directive).toBeTruthy();
  });
});
