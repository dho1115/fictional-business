import { SetIvDirective } from './set-iv.directive';

describe('SetIvDirective', () => {
  it('should create an instance', () => {
    const directive = new SetIvDirective();
    expect(directive).toBeTruthy();
  });
});
