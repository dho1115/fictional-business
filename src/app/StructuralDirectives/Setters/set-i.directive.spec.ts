import { SetIDirective } from './set-i.directive';

describe('SetIDirective', () => {
  it('should create an instance', () => {
    const directive = new SetIDirective();
    expect(directive).toBeTruthy();
  });
});
