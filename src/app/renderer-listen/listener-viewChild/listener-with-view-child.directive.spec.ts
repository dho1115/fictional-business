import { ListenerWithViewChildDirective } from './listener-with-view-child.directive';

describe('ListenerWithViewChildDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenerWithViewChildDirective();
    expect(directive).toBeTruthy();
  });
});
