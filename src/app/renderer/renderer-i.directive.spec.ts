import { RendererIDirective } from './renderer-i.directive';

describe('appRendererI', () => {
  it('should create an instance', () => {
    const directive = new RendererIDirective();
    expect(directive).toBeTruthy();
  });
});
